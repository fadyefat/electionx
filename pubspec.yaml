name: election
description:
publish_to:

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  reown_appkit: ^1.4.2+1
  web3dart: ^2.3.0
  http: ^1.3.0
  flutter_web3: ^2.1.9
  shared_preferences: ^2.0.15


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/MetaMask_Fox.svg.png
    - assets/Election.json